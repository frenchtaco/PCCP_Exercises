# Running JUnit tests

* JUnit tests must be located in the directory `src/test/java/exercises05/`.

* To run JUnit tests with Gradle, execute `$ gradle cleanTest test --tests <package>.<test_class>` from the root of the project. For instance, `$ gradle cleanTest test --tests exercises05.ConcurrentSetTest`.
